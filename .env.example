# ===========================================
# SLACK CONFIGURATION (for Slack bots)
# ===========================================
# Required for: src/bot.js, src/opencode-bot.js

# Slack Bot Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Signing Secret (from app settings)
SLACK_SIGNING_SECRET=your-signing-secret

# Slack App Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# ===========================================
# MICROSOFT TEAMS CONFIGURATION (for Teams bots)
# ===========================================
# Required for: src/teams-bot.js, src/teams-opencode-bot.js
# Get these from Teams Developer Portal (dev.teams.microsoft.com)

# Microsoft App ID (Bot ID from Teams Developer Portal → Tools → Bot management)
MICROSOFT_APP_ID=your-bot-id-here

# Microsoft App Password (client secret from Bot management)
MICROSOFT_APP_PASSWORD=your-client-secret-here

# Bot server port (default: 3978)
PORT=3978

# REQUIRED: Tenant ID for Teams Developer Portal bots (single-tenant by default)
# Find at: Azure Portal → Azure Active Directory → Overview → Tenant ID
MICROSOFT_APP_TENANT_ID=your-tenant-id-here

# ===========================================
# OPENCODE CONFIGURATION (for OpenCode bots)
# ===========================================
# Required for: src/opencode-bot.js, src/teams-opencode-bot.js

# Optional: Specify which model to use (e.g., anthropic/claude-sonnet-4-20250514, openai/gpt-4o)
# If not set, OpenCode uses its default model
# OPENCODE_MODEL=anthropic/claude-sonnet-4-20250514

# Optional: Full path to OpenCode binary (if not in PATH)
# OPENCODE_PATH=/home/yourusername/.opencode/bin/opencode

# ===========================================
# WHICH BOT TO RUN?
# ===========================================
# 
# Slack + Claude Code:    npm start              (src/bot.js)
# Slack + OpenCode:       npm run start:opencode (src/opencode-bot.js)
# Teams + Claude Code:    npm run start:teams    (src/teams-bot.js)
# Teams + OpenCode:       npm run start:teams:opencode (src/teams-opencode-bot.js)
